# Write your MySQL query statement below
select sell_date,count(sell_date) as num_sold, group_concat(distinct product order by product separator ',') as products from (select distinct * from activities) as tab group by date(sell_date) order by date(sell_date)
