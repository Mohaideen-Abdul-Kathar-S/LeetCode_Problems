class Solution {
    public int repeatedNTimes(int[] nums) {
        List<Integer> ls = new ArrayList<>();
        for(int i:nums){
            if(ls.contains(i)) return i;
            ls.add(i);
        }
        return 0;
    }
}
