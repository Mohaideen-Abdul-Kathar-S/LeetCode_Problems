class Solution {
    public long maximumHappinessSum(int[] hap, int k) {
       
        long sum=0,j=0;
        int n=hap.length;
    Arrays.sort(hap);
    for(int i=n-1;i>=n-k;i--){
            if(hap[i]-j<=0) break;
        sum = sum + (long) hap[i]-j;
        j++;
    }
    return sum;
    }
}
